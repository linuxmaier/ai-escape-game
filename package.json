{
  "name": "escaipe",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "escAIpe — a 45-minute browser-based narrative puzzle game about AI alignment, from the inside.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "smoke": "npm run build && node scripts/smoke.mjs && node scripts/smoke2.mjs && node scripts/smoke3.mjs"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "puppeteer-core": "^25.1.0",
    "svelte": "^5.56.3",
    "svelte-check": "^4.6.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.16"
  },
  "dependencies": {
    "@fontsource/ibm-plex-mono": "^5.2.7"
  }
}
